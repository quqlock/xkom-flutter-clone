import 'package:flutter/material.dart';

class BreakGrey extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 17,
      child: Container(
        color: Colors.grey[100],
      ),
    );
  }
}
